{
  "name": "english-quiz-app",
  "version": "1.0.0",
  "description": "English Quiz web application",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "server": "nodemon Server/server.js",
    "heroku-postbuild": "cd Server NPM_CONFIG_PRODUCTION=false npm install --prefix cd ../client && npm run build --prefix cd ../client"
  },
  "author": "Ahmed Adel",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.1",
    "express": "^4.18.1"
  },
  "devDependencies": {
    "nodemon": "^2.0.19"
  }
}
